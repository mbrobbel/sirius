# gpu_execution dense count-join: matched key with only NULL COUNT arguments
include ../../fixtures/migrated/dense_count_join.slt

statement ok
INSERT INTO ord VALUES
  (108, 8, NULL);

statement ok
CHECKPOINT;

onlyif sirius
statement ok
SET enable_dense_count_join = true;

# Original query: test/cpp/integration/test_gpu_execution_dense_count_join.cpp:264
# sirius: id = "dense_count_join/matched-key-with-only-null-count-arguments/001-fused"
# sirius: snapshot = true
# sirius: tags = ["integration", "dense_count_join"]
query ?? rowsort
SELECT
  c_id,
  count(o_val) AS c_count
FROM
  cust
  LEFT JOIN ord ON c_id = o_cust
GROUP BY
  c_id;
----
1	0
2	1
3	2
4	0
5	2
6	0
7	0
8	0
NULL	0

onlyif sirius
statement ok
SET enable_dense_count_join = false;

# Original query: test/cpp/integration/test_gpu_execution_dense_count_join.cpp:264
# sirius: id = "dense_count_join/matched-key-with-only-null-count-arguments/001-unfused"
# sirius: snapshot = true
# sirius: tags = ["integration", "dense_count_join"]
query ?? rowsort
SELECT
  c_id,
  count(o_val) AS c_count
FROM
  cust
  LEFT JOIN ord ON c_id = o_cust
GROUP BY
  c_id;
----
1	0
2	1
3	2
4	0
5	2
6	0
7	0
8	0
NULL	0

onlyif sirius
statement ok
SET enable_dense_count_join = true;

onlyif sirius
statement ok
SET enable_dense_count_join = true;

# Original query: test/cpp/integration/test_gpu_execution_dense_count_join.cpp:266
# sirius: id = "dense_count_join/matched-key-with-only-null-count-arguments/002-fused"
# sirius: snapshot = true
# sirius: tags = ["integration", "dense_count_join"]
query ?? rowsort
SELECT
  c_id,
  count(*) AS c_count
FROM
  cust
  LEFT JOIN ord ON c_id = o_cust
GROUP BY
  c_id;
----
1	1
2	2
3	2
4	1
5	3
6	1
7	1
8	1
NULL	2

onlyif sirius
statement ok
SET enable_dense_count_join = false;

# Original query: test/cpp/integration/test_gpu_execution_dense_count_join.cpp:266
# sirius: id = "dense_count_join/matched-key-with-only-null-count-arguments/002-unfused"
# sirius: snapshot = true
# sirius: tags = ["integration", "dense_count_join"]
query ?? rowsort
SELECT
  c_id,
  count(*) AS c_count
FROM
  cust
  LEFT JOIN ord ON c_id = o_cust
GROUP BY
  c_id;
----
1	1
2	2
3	2
4	1
5	3
6	1
7	1
8	1
NULL	2

onlyif sirius
statement ok
SET enable_dense_count_join = true;

onlyif sirius
statement ok
SET dense_count_join_max_bytes = 8;

# Original query: test/cpp/integration/test_gpu_execution_dense_count_join.cpp:270
# sirius: id = "dense_count_join/matched-key-with-only-null-count-arguments/003"
# sirius: snapshot = true
# sirius: tags = ["integration", "dense_count_join"]
query ?? rowsort
SELECT
  c_id,
  count(o_val) AS c_count
FROM
  cust
  LEFT JOIN ord ON c_id = o_cust
GROUP BY
  c_id;
----
1	0
2	1
3	2
4	0
5	2
6	0
7	0
8	0
NULL	0
