# gpu_execution dense count-join: RIGHT-join orientation with a nested preserved input
include ../../fixtures/migrated/dense_count_join.slt

onlyif sirius
statement ok
SET enable_dense_count_join = true;

# Original query: test/cpp/integration/test_gpu_execution_dense_count_join.cpp:219
# sirius: id = "dense_count_join/right-join-orientation-with-a-nested-preserved-input/001-fused"
# sirius: snapshot = true
# sirius: tags = ["integration", "dense_count_join"]
query ?? rowsort
SELECT
  c.c_id,
  count(o_id) AS c_count
FROM
  ord
  RIGHT JOIN (SELECT c1.c_id FROM cust c1 JOIN cust c2 ON c1.c_grp = c2.c_grp) c ON o_cust = c.c_id
GROUP BY
  c.c_id;
----
1	0
2	10
3	11
4	0
5	15
6	0
7	0
8	0
NULL	0

onlyif sirius
statement ok
SET enable_dense_count_join = false;

# Original query: test/cpp/integration/test_gpu_execution_dense_count_join.cpp:219
# sirius: id = "dense_count_join/right-join-orientation-with-a-nested-preserved-input/001-unfused"
# sirius: snapshot = true
# sirius: tags = ["integration", "dense_count_join"]
query ?? rowsort
SELECT
  c.c_id,
  count(o_id) AS c_count
FROM
  ord
  RIGHT JOIN (SELECT c1.c_id FROM cust c1 JOIN cust c2 ON c1.c_grp = c2.c_grp) c ON o_cust = c.c_id
GROUP BY
  c.c_id;
----
1	0
2	10
3	11
4	0
5	15
6	0
7	0
8	0
NULL	0

onlyif sirius
statement ok
SET enable_dense_count_join = true;
