# gpu_execution array - NULL arrays carry list-level validity
# Preserve setup and query order from the C++ execution test.

statement ok
CREATE TABLE arr_null (
  id INTEGER,
  a INTEGER[3]
);

statement ok
INSERT INTO arr_null VALUES
  (1, [1, 2, 3]),
  (2, NULL),
  (3, [7, 8, 9]),
  (4, NULL);

statement ok
CHECKPOINT;

# Original query: test/cpp/integration/test_gpu_execution_array.cpp:309
# sirius: id = "array/null-arrays-carry-list-level-validity/001"
# sirius: tags = ["integration", "array"]
query ?? rowsort
SELECT
  id,
  a
FROM
  arr_null;
----
