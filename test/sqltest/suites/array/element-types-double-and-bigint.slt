# gpu_execution array - element types DOUBLE and BIGINT
# Preserve setup and query order from the C++ execution test.

statement ok
CREATE TABLE arr_dbl (
  id INTEGER,
  a DOUBLE[2]
);

statement ok
INSERT INTO arr_dbl VALUES
  (1, [1.5, 2.5]),
  (2, [3.25, 4.75]);

statement ok
CHECKPOINT;

# Original query: test/cpp/integration/test_gpu_execution_array.cpp:131
# sirius: id = "array/element-types-double-and-bigint/001"
# sirius: tags = ["integration", "array"]
query ?? rowsort
SELECT
  id,
  a
FROM
  arr_dbl;
----

statement ok
CREATE TABLE arr_big (
  id INTEGER,
  a BIGINT[4]
);

statement ok
INSERT INTO arr_big VALUES
  (1, [100, 200, 300, 400]),
  (2, [500, 600, 700, 800]);

statement ok
CHECKPOINT;

# Original query: test/cpp/integration/test_gpu_execution_array.cpp:136
# sirius: id = "array/element-types-double-and-bigint/002"
# sirius: tags = ["integration", "array"]
query ?? rowsort
SELECT
  id,
  a
FROM
  arr_big;
----
