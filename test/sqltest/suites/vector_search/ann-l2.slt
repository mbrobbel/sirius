statement ok
CREATE TABLE vs_l2 AS SELECT i AS id, [i, i, i]::FLOAT[3] AS vec FROM range(5000) t (i);

statement ok
CHECKPOINT;

onlyif sirius
statement ok
SELECT
  *
FROM
  pin_table(name => 'vs_l2', tier => 'gpu', format => 'duckdb');

onlyif sirius
statement ok
SELECT
  *
FROM
  sirius_create_ann_index('vs_l2', 'vec', metric => 'l2', n_lists => 16);

onlyif duckdb
statement ok
CREATE OR REPLACE VIEW search_result AS
SELECT
  id
FROM
  vs_l2 ORDER BY array_distance(vec, [0.0, 0.0, 0.0]::FLOAT[3]) LIMIT 1;

onlyif sirius
statement ok
CREATE OR REPLACE VIEW search_result AS
SELECT
  id
FROM
  sirius_knn_search('vs_l2', 'vec', [0.0, 0.0, 0.0]::FLOAT[3], k => 1, output_columns => ['id']);

# Original query: test/cpp/integration/test_gpu_execution_vector_search.cpp:97
# sirius: id = "vector_search/ann-l2/001"
# sirius: tags = ["integration", "vector_search", "explicit_gpu_function"]
# sirius: execution = "allow_fallback"
query ? rowsort
SELECT
  *
FROM
  search_result;
----

onlyif duckdb
statement ok
CREATE OR REPLACE VIEW search_result AS
SELECT
  id
FROM
  vs_l2 ORDER BY array_distance(vec, [0.0, 0.0, 0.0]::FLOAT[3]) LIMIT 5;

onlyif sirius
statement ok
CREATE OR REPLACE VIEW search_result AS
SELECT
  id
FROM
  sirius_knn_search('vs_l2', 'vec', [0.0, 0.0, 0.0]::FLOAT[3], k => 5, output_columns => ['id']);

# Original query: test/cpp/integration/test_gpu_execution_vector_search.cpp:97
# sirius: id = "vector_search/ann-l2/002"
# sirius: tags = ["integration", "vector_search", "explicit_gpu_function"]
# sirius: execution = "allow_fallback"
query ? rowsort
SELECT
  *
FROM
  search_result;
----

onlyif duckdb
statement ok
CREATE OR REPLACE VIEW search_result AS
SELECT
  id
FROM
  vs_l2 ORDER BY array_distance(vec, [0.0, 0.0, 0.0]::FLOAT[3]) LIMIT 20;

onlyif sirius
statement ok
CREATE OR REPLACE VIEW search_result AS
SELECT
  id
FROM
  sirius_knn_search('vs_l2', 'vec', [0.0, 0.0, 0.0]::FLOAT[3], k => 20, output_columns => ['id']);

# Original query: test/cpp/integration/test_gpu_execution_vector_search.cpp:97
# sirius: id = "vector_search/ann-l2/003"
# sirius: tags = ["integration", "vector_search", "explicit_gpu_function"]
# sirius: execution = "allow_fallback"
query ? rowsort
SELECT
  *
FROM
  search_result;
----

onlyif duckdb
statement ok
CREATE OR REPLACE VIEW search_result AS
SELECT
  id
FROM
  vs_l2 ORDER BY array_distance(vec, [0.0, 0.0, 0.0]::FLOAT[3]) LIMIT 100;

onlyif sirius
statement ok
CREATE OR REPLACE VIEW search_result AS
SELECT
  id
FROM
  sirius_knn_search('vs_l2', 'vec', [0.0, 0.0, 0.0]::FLOAT[3], k => 100, output_columns => ['id']);

# Original query: test/cpp/integration/test_gpu_execution_vector_search.cpp:97
# sirius: id = "vector_search/ann-l2/004"
# sirius: tags = ["integration", "vector_search", "explicit_gpu_function"]
# sirius: execution = "allow_fallback"
query ? rowsort
SELECT
  *
FROM
  search_result;
----

onlyif duckdb
statement ok
CREATE OR REPLACE VIEW search_result AS
SELECT
  id
FROM
  vs_l2 ORDER BY array_distance(vec, [1000.0, 1000.0, 1000.0]::FLOAT[3]) LIMIT 1;

onlyif sirius
statement ok
CREATE OR REPLACE VIEW search_result AS
SELECT
  id
FROM
  sirius_knn_search('vs_l2', 'vec', [1000.0, 1000.0, 1000.0]::FLOAT[3], k => 1, output_columns => ['id']);

# Original query: test/cpp/integration/test_gpu_execution_vector_search.cpp:105
# sirius: id = "vector_search/ann-l2/005"
# sirius: tags = ["integration", "vector_search", "explicit_gpu_function"]
# sirius: execution = "allow_fallback"
query ? rowsort
SELECT
  *
FROM
  search_result;
----

onlyif duckdb
statement ok
CREATE OR REPLACE VIEW search_result AS
SELECT
  id
FROM
  vs_l2 ORDER BY array_distance(vec, [1000.0, 1000.0, 1000.0]::FLOAT[3]) LIMIT 7;

onlyif sirius
statement ok
CREATE OR REPLACE VIEW search_result AS
SELECT
  id
FROM
  sirius_knn_search('vs_l2', 'vec', [1000.0, 1000.0, 1000.0]::FLOAT[3], k => 7, output_columns => ['id']);

# Original query: test/cpp/integration/test_gpu_execution_vector_search.cpp:105
# sirius: id = "vector_search/ann-l2/006"
# sirius: tags = ["integration", "vector_search", "explicit_gpu_function"]
# sirius: execution = "allow_fallback"
query ? rowsort
SELECT
  *
FROM
  search_result;
----

onlyif duckdb
statement ok
CREATE OR REPLACE VIEW search_result AS
SELECT
  id
FROM
  vs_l2 ORDER BY array_distance(vec, [1000.0, 1000.0, 1000.0]::FLOAT[3]) LIMIT 21;

onlyif sirius
statement ok
CREATE OR REPLACE VIEW search_result AS
SELECT
  id
FROM
  sirius_knn_search('vs_l2', 'vec', [1000.0, 1000.0, 1000.0]::FLOAT[3], k => 21, output_columns => ['id']);

# Original query: test/cpp/integration/test_gpu_execution_vector_search.cpp:105
# sirius: id = "vector_search/ann-l2/007"
# sirius: tags = ["integration", "vector_search", "explicit_gpu_function"]
# sirius: execution = "allow_fallback"
query ? rowsort
SELECT
  *
FROM
  search_result;
----

onlyif sirius
statement ok
SELECT
  *
FROM
  unpin_table('vs_l2');
