statement ok
CREATE TABLE vs_mc AS SELECT i AS id, [v, v, v]::FLOAT[3] AS vec FROM (SELECT i, (i * 7) % 200000 AS v FROM range(200000) t (i));

statement ok
CHECKPOINT;

onlyif sirius
statement ok
SET scan_task_batch_size = 4096;

onlyif sirius
statement ok
SELECT
  *
FROM
  pin_table(name => 'vs_mc', tier => 'gpu', format => 'duckdb');

onlyif sirius
statement ok
SET scan_task_batch_size = 536870912;

onlyif sirius
statement ok
SELECT
  *
FROM
  sirius_create_ann_index('vs_mc', 'vec', metric => 'l2', n_lists => 16);

onlyif duckdb
statement ok
CREATE OR REPLACE VIEW search_result AS
SELECT
  id
FROM
  vs_mc ORDER BY array_distance(vec, [0.0, 0.0, 0.0]::FLOAT[3]) LIMIT 1;

onlyif sirius
statement ok
CREATE OR REPLACE VIEW search_result AS
SELECT
  id
FROM
  sirius_knn_search('vs_mc', 'vec', [0.0, 0.0, 0.0]::FLOAT[3], k => 1, output_columns => ['id'], use_index => false);

# Original query: test/cpp/integration/test_gpu_execution_vector_search.cpp:955
# sirius: id = "vector_search/multi-chunk-enn/001"
# sirius: tags = ["integration", "vector_search", "explicit_gpu_function"]
# sirius: execution = "allow_fallback"
query ? rowsort
SELECT
  *
FROM
  search_result;
----

onlyif duckdb
statement ok
CREATE OR REPLACE VIEW search_result AS
SELECT
  id
FROM
  vs_mc ORDER BY array_distance(vec, [0.0, 0.0, 0.0]::FLOAT[3]) LIMIT 5;

onlyif sirius
statement ok
CREATE OR REPLACE VIEW search_result AS
SELECT
  id
FROM
  sirius_knn_search('vs_mc', 'vec', [0.0, 0.0, 0.0]::FLOAT[3], k => 5, output_columns => ['id'], use_index => false);

# Original query: test/cpp/integration/test_gpu_execution_vector_search.cpp:955
# sirius: id = "vector_search/multi-chunk-enn/002"
# sirius: tags = ["integration", "vector_search", "explicit_gpu_function"]
# sirius: execution = "allow_fallback"
query ? rowsort
SELECT
  *
FROM
  search_result;
----

onlyif duckdb
statement ok
CREATE OR REPLACE VIEW search_result AS
SELECT
  id
FROM
  vs_mc ORDER BY array_distance(vec, [0.0, 0.0, 0.0]::FLOAT[3]) LIMIT 50;

onlyif sirius
statement ok
CREATE OR REPLACE VIEW search_result AS
SELECT
  id
FROM
  sirius_knn_search('vs_mc', 'vec', [0.0, 0.0, 0.0]::FLOAT[3], k => 50, output_columns => ['id'], use_index => false);

# Original query: test/cpp/integration/test_gpu_execution_vector_search.cpp:955
# sirius: id = "vector_search/multi-chunk-enn/003"
# sirius: tags = ["integration", "vector_search", "explicit_gpu_function"]
# sirius: execution = "allow_fallback"
query ? rowsort
SELECT
  *
FROM
  search_result;
----

onlyif duckdb
statement ok
CREATE OR REPLACE VIEW search_result AS
SELECT
  id
FROM
  vs_mc ORDER BY array_distance(vec, [0.0, 0.0, 0.0]::FLOAT[3]) LIMIT 500;

onlyif sirius
statement ok
CREATE OR REPLACE VIEW search_result AS
SELECT
  id
FROM
  sirius_knn_search('vs_mc', 'vec', [0.0, 0.0, 0.0]::FLOAT[3], k => 500, output_columns => ['id'], use_index => false);

# Original query: test/cpp/integration/test_gpu_execution_vector_search.cpp:955
# sirius: id = "vector_search/multi-chunk-enn/004"
# sirius: tags = ["integration", "vector_search", "explicit_gpu_function"]
# sirius: execution = "allow_fallback"
query ? rowsort
SELECT
  *
FROM
  search_result;
----

onlyif sirius
statement ok
SELECT
  *
FROM
  unpin_table('vs_mc');
