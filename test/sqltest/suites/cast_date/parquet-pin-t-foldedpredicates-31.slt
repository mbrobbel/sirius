include ../../fixtures/migrated/cast_date.slt

onlyif sirius
statement ok
SET pin_table_compression = true;

onlyif sirius
statement ok
SET pin_table_compression_min_batch_size_bytes = 0;

onlyif sirius
statement ok
SET pin_table_compression_max_compressed_fraction = 1000;

onlyif sirius
statement ok
SET pin_table_input_compression_plan_dir = '__TEST_DIR__';

onlyif sirius
statement ok
CALL pin_table('__TEST_DIR__/t.parquet', tier = 'gpu', name = 'p_t');

# Original predicate: test/cpp/integration/test_gpu_execution_cast_date_predicates.cpp:242
# sirius: id = "cast_date/parquet-pin-t-foldedpredicates-31/ids"
# sirius: tags = ["integration", "cast_date", "fused_scan_filter", "parquet-pin"]
query ? rowsort
SELECT
  id
FROM
  p_t
WHERE
  d <= DATE '1998-09-20';
----

# Original predicate: test/cpp/integration/test_gpu_execution_cast_date_predicates.cpp:242
# sirius: id = "cast_date/parquet-pin-t-foldedpredicates-31/aggregate"
# sirius: tags = ["integration", "cast_date", "fused_scan_filter", "parquet-pin"]
query ?? rowsort
SELECT
  count(*),
  sum(id)
FROM
  p_t
WHERE
  d <= DATE '1998-09-20';
----

onlyif sirius
statement ok
CALL unpin_table('p_t');
