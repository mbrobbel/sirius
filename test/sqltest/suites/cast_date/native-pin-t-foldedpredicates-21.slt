include ../../fixtures/migrated/cast_date.slt

onlyif sirius
statement ok
CALL pin_table(format = 'duckdb', name = 't', tier = 'gpu');

# Original predicate: test/cpp/integration/test_gpu_execution_cast_date_predicates.cpp:228
# sirius: id = "cast_date/native-pin-t-foldedpredicates-21/ids"
# sirius: tags = ["integration", "cast_date", "fused_scan_filter", "native-pin"]
query ? rowsort
SELECT
  id
FROM
  t
WHERE
  TIMESTAMP '1998-09-20 00:00:00' < d;
----

# Original predicate: test/cpp/integration/test_gpu_execution_cast_date_predicates.cpp:228
# sirius: id = "cast_date/native-pin-t-foldedpredicates-21/aggregate"
# sirius: tags = ["integration", "cast_date", "fused_scan_filter", "native-pin"]
query ?? rowsort
SELECT
  count(*),
  sum(id)
FROM
  t
WHERE
  TIMESTAMP '1998-09-20 00:00:00' < d;
----

onlyif sirius
statement ok
CALL unpin_table('t');
