# parquet nulls — numeric column with nulls aggregates
include ../../fixtures/migrated/parquet_nulls/ParquetDictNullFixture.slt

# Original query: test/cpp/integration/test_gpu_execution_parquet_nulls.cpp:935
# sirius: id = "parquet_nulls/numeric-column-with-nulls-aggregates/001"
# sirius: tags = ["integration", "parquet", "nulls"]
query ????? rowsort
SELECT
  COUNT(*),
  COUNT(val),
  SUM(val),
  MIN(val),
  MAX(val)
FROM
  read_parquet('__TEST_DIR__/dict_nulls.parquet');
----
