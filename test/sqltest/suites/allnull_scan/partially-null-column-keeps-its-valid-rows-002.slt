# Migrated from test/cpp/integration/test_gpu_execution_allnull_scan.cpp:88
# gpu_execution partially-NULL column keeps its valid rows
include ../../fixtures/migrated/allnull_scan/8f09309befc2edf2.slt

# sirius: id = "allnull_scan/partially-null-column-keeps-its-valid-rows/002"
# sirius: snapshot = true
# sirius: tags = ["allnull", "gpu_execution", "integration", "nulls", "scan"]
query ???? rowsort
SELECT
  COUNT(part),
  SUM(part),
  MIN(part),
  MAX(part)
FROM
  ans;
----
4	20	2	8
