# Migrated from test/cpp/integration/test_gpu_execution_allnull_scan.cpp:114
# gpu_execution constant-valued run then all-NULL run in one row group
include ../../fixtures/migrated/allnull_scan/450669759e7ffc55.slt

# sirius: id = "allnull_scan/constant-valued-run-then-all-null-run-in-one-row-group/001"
# sirius: snapshot = true
# sirius: tags = ["allnull", "gpu_execution", "integration", "nulls", "scan"]
query ????? rowsort
SELECT
  COUNT(*),
  COUNT(c),
  SUM(c),
  MIN(c),
  MAX(c)
FROM
  mix;
----
1000	500	3500	7	7
