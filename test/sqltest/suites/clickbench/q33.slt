# Source and license: ../../upstream/README.md
include ../../fixtures/clickbench.slt

# sirius: id = "clickbench/q33"
# sirius: tags = ["clickbench"]
# sirius: tolerances = { "4" = { absolute = 1e-8, relative = 1e-9 } }

query ????? nosort
SELECT WatchID, ClientIP, COUNT(*) AS c, SUM(IsRefresh), AVG(ResolutionWidth) FROM hits GROUP BY WatchID, ClientIP ORDER BY c DESC, 1, 2, 3, 4, 5 LIMIT 10;
----
