# Source and license: ../../upstream/README.md
include ../../fixtures/clickbench.slt

# sirius: id = "clickbench/q26"
# sirius: tags = ["clickbench"]

query ? nosort
SELECT SearchPhrase FROM hits WHERE SearchPhrase <> '' ORDER BY SearchPhrase, 1 LIMIT 10;
----
