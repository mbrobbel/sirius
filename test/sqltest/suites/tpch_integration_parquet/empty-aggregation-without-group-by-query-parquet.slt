# gpu_execution - empty aggregation without group by query parquet
include ../../fixtures/migrated/tpch-parquet-original.slt

# Original query: test/cpp/integration/test_gpu_execution_tpch.cpp:3494
# sirius: id = "tpch_integration_parquet/empty-aggregation-without-group-by-query-parquet/001"
# sirius: tags = ["integration", "gpu_execution", "parquet", "empty_result"]
query ???????? rowsort
SELECT
  count(*),
  min(l_orderkey),
  sum(l_orderkey) AS sumo,
  count(l_orderkey),
  count(l_receiptdate),
  min(l_receiptdate),
  count(l_comment),
  min(l_comment)
FROM
  lineitem
WHERE
  l_linestatus = 'J';
----
