# gpu_execution - mark join via OR + IN subquery parquet
include ../../fixtures/migrated/tpch-parquet-original.slt

# Original query: test/cpp/integration/test_gpu_execution_tpch.cpp:1727
# sirius: id = "tpch_integration_parquet/mark-join-via-or-in-subquery-parquet/001"
# sirius: tags = ["integration", "gpu_execution", "parquet", "markjoin"]
query ? rowsort
SELECT
  count(*) AS n
FROM
  orders
WHERE
  o_orderkey < 0 OR o_custkey IN (SELECT c_custkey FROM customer WHERE c_nationkey < 3);
----
