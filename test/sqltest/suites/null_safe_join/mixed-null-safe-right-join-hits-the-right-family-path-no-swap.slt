# gpu_execution mixed '=' + null-safe RIGHT join hits the right-family path (no swap)
include ../../fixtures/migrated/null_safe_join/MixedKeyNullSafeJoinFixture.slt

statement ok
SET disabled_optimizers = concat_ws(',', nullif(current_setting('disabled_optimizers'), ''), 'build_side_probe_side');

# Original query: test/cpp/integration/test_gpu_execution_null_safe_join.cpp:212
# sirius: id = "null_safe_join/mixed-null-safe-right-join-hits-the-right-family-path-no-swap/001"
# sirius: snapshot = true
# sirius: tags = ["integration", "join", "nulls", "null_safe"]
query ?? rowsort
SELECT
  mnl.id,
  mnr.id
FROM
  mnl
  RIGHT JOIN mnr ON mnl.a = mnr.a AND mnl.b IS NOT DISTINCT FROM mnr.b;
----
1	100
2	101
3	102
NULL	103
NULL	104
