# gpu_execution - two group by key: min max, sum, count of doubles parquet
include ../../fixtures/migrated/tpch-parquet-original.slt

# Original query: test/cpp/integration/test_gpu_execution_tpch.cpp:2723
# sirius: id = "tpch_disabled_parquet/two-group-by-key-min-max-sum-count-of-doubles-parquet/001"
# sirius: tags = ["integration_disabled", "gpu_execution", "parquet", "aggregate", "previously_disabled"]
query ?????? rowsort
SELECT
  c_nationkey,
  c_mktsegment,
  min(C_ACCTBAL),
  max(C_ACCTBAL),
  sum(C_ACCTBAL),
  count(C_ACCTBAL)
FROM
  customer
GROUP BY
  c_nationkey, c_mktsegment;
----
