# gpu_execution - empty native table count identity

statement ok
ATTACH '__FIXTURE_ROOT__/tpch-native-original/integration.duckdb' AS tpch (READ_ONLY);

statement ok
CREATE TABLE e (
  i INTEGER
);

statement ok
CHECKPOINT;

# Original query: test/cpp/integration/test_gpu_execution_tpch.cpp:3909
# sirius: id = "tpch_empty/empty-native-table-count-identity"
# sirius: tags = ["integration", "tpch", "empty_result"]
# sirius: snapshot = true
# sirius: timeout = 30
query I nosort
SELECT
  count(*) AS c
FROM
  e;
----
0
