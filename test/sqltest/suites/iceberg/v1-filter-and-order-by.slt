# gpu_execution iceberg - V1 filter and order by

statement ok
LOAD avro;

statement ok
LOAD iceberg;

statement ok
SET unsafe_enable_version_guessing = true;

statement ok
CREATE TEMPORARY TABLE fixture_check (
  ok BOOLEAN NOT NULL CHECK (ok)
);

# Original case: test/cpp/integration/test_gpu_execution_multi_format.cpp:1176
# sirius: id = "iceberg/v1-filter-and-order-by/001"
# sirius: tags = ["integration", "iceberg"]
# sirius: snapshot = true
query ?? rowsort
SELECT
  fruit,
  count
FROM
  iceberg_scan('__TEST_DIR__/test/cpp/integration/data/iceberg_v1', snapshot_from_id = 1000000000000000001)
WHERE
  count > 1 ORDER BY count;
----
"banana"	2
"cherry"	3

statement ok
DROP TABLE fixture_check;
