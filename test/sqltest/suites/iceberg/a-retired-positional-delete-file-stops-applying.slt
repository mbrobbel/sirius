# gpu_execution iceberg - a retired positional-delete file stops applying

statement ok
LOAD avro;

statement ok
LOAD iceberg;

statement ok
SET unsafe_enable_version_guessing = true;

statement ok
CREATE TEMPORARY TABLE fixture_check (
  ok BOOLEAN NOT NULL CHECK (ok)
);

statement ok
INSERT INTO fixture_check SELECT
  count(*) = 1
FROM
  iceberg_metadata('__TEST_DIR__/test/cpp/integration/data/iceberg_v2_pos_delete_retired')
WHERE
  content <> 'EXISTING';

# Original case: test/cpp/integration/test_gpu_execution_multi_format.cpp:1516
# sirius: id = "iceberg/a-retired-positional-delete-file-stops-applying/001"
# sirius: tags = ["integration", "iceberg"]
# sirius: snapshot = true
query ?? rowsort
SELECT
  fruit,
  count
FROM
  iceberg_scan('__TEST_DIR__/test/cpp/integration/data/iceberg_v2_pos_delete_retired', snapshot_from_id = 2000000000000000001) ORDER BY count;
----
"apple"	1
"banana"	2
"cherry"	3
"date"	4
"elderberry"	5

statement ok
DROP TABLE fixture_check;
