# gpu_execution iceberg - deflate compressed manifests

statement ok
LOAD avro;

statement ok
LOAD iceberg;

statement ok
SET unsafe_enable_version_guessing = true;

statement ok
CREATE TEMPORARY TABLE fixture_check (
  ok BOOLEAN NOT NULL CHECK (ok)
);

statement ok
INSERT INTO fixture_check SELECT
  count(*) = 0
FROM
  iceberg_metadata('__TEST_DIR__/test/cpp/integration/data/iceberg_deflate_manifest')
WHERE
  content <> 'EXISTING';

# Original case: test/cpp/integration/test_gpu_execution_multi_format.cpp:1646
# sirius: id = "iceberg/deflate-compressed-manifests/001"
# sirius: tags = ["integration", "iceberg"]
# sirius: snapshot = true
query ?? rowsort
SELECT
  *
FROM
  iceberg_scan('__TEST_DIR__/test/cpp/integration/data/iceberg_deflate_manifest', snapshot_from_id = 9500000000000001) ORDER BY count;
----
"apple"	1
"banana"	2
"cherry"	3

statement ok
DROP TABLE fixture_check;
