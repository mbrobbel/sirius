# gpu_execution iceberg - V3 deletion vector count(*)

statement ok
LOAD avro;

statement ok
LOAD iceberg;

statement ok
SET unsafe_enable_version_guessing = true;

statement ok
CREATE TEMPORARY TABLE fixture_check (
  ok BOOLEAN NOT NULL CHECK (ok)
);

statement ok
INSERT INTO fixture_check SELECT
  count(*) = 1
FROM
  iceberg_metadata('__TEST_DIR__/test/cpp/integration/data/iceberg_v3_deletion_vector')
WHERE
  content <> 'EXISTING';

# Original case: test/cpp/integration/test_gpu_execution_multi_format.cpp:1388
# sirius: id = "iceberg/v3-deletion-vector-count/001"
# sirius: tags = ["integration", "iceberg"]
# sirius: snapshot = true
query ? rowsort
SELECT
  count(*)
FROM
  iceberg_scan('__TEST_DIR__/test/cpp/integration/data/iceberg_v3_deletion_vector', snapshot_from_id = 7000000000000000001);
----
3

statement ok
DROP TABLE fixture_check;
