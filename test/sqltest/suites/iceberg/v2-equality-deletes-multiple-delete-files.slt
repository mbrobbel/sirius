# gpu_execution iceberg - V2 equality deletes multiple delete files

statement ok
LOAD avro;

statement ok
LOAD iceberg;

statement ok
SET unsafe_enable_version_guessing = true;

statement ok
CREATE TEMPORARY TABLE fixture_check (
  ok BOOLEAN NOT NULL CHECK (ok)
);

statement ok
INSERT INTO fixture_check SELECT
  count(*) = 2
FROM
  iceberg_metadata('__TEST_DIR__/test/cpp/integration/data/iceberg_v2_eq_multi_delete_file')
WHERE
  content <> 'EXISTING';

# Original case: test/cpp/integration/test_gpu_execution_multi_format.cpp:1309
# sirius: id = "iceberg/v2-equality-deletes-multiple-delete-files/001"
# sirius: tags = ["integration", "iceberg"]
# sirius: snapshot = true
# sirius: execution = "allow_fallback"
query ?? rowsort
SELECT
  fruit,
  count
FROM
  iceberg_scan('__TEST_DIR__/test/cpp/integration/data/iceberg_v2_eq_multi_delete_file', snapshot_from_id = 5000000000000000001) ORDER BY count;
----
"apple"	1
"cherry"	3
"elderberry"	5

statement ok
DROP TABLE fixture_check;
