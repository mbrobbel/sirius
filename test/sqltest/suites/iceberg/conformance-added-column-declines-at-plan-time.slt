# gpu_execution iceberg - conformance added column declines at plan time

statement ok
LOAD avro;

statement ok
LOAD iceberg;

statement ok
SET GLOBAL unsafe_enable_version_guessing = true;

statement ok
CREATE TEMPORARY TABLE fixture_check (
  ok BOOLEAN NOT NULL CHECK (ok)
);

# Original case: test/cpp/integration/test_gpu_execution_multi_format.cpp:1061
# sirius: id = "iceberg/conformance-added-column-declines-at-plan-time/001"
# sirius: timeout = 90
# sirius: tags = ["integration", "iceberg"]
# sirius: snapshot = true
# sirius: execution = "allow_fallback"
query ??? rowsort
SELECT
  id,
  a,
  b
FROM
  iceberg_scan('__TEST_DIR__/test/cpp/integration/data/iceberg_conformance/add_column/conf/add_column', snapshot_from_id = 6204621527650249004) ORDER BY id;
----
1	10	NULL
2	20	NULL
3	30	300
4	40	400

# sirius: id = "iceberg/conformance-added-column-declines-at-plan-time/liveness"
# sirius: tags = ["integration", "iceberg", "liveness"]
# sirius: snapshot = true
# sirius: execution = "allow_fallback"
# sirius: timeout = 90
query T
SELECT
  'LIVENESS_OK' AS probe;
----
"LIVENESS_OK"

statement ok
DROP TABLE fixture_check;
