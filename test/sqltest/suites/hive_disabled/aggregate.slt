# gpu_execution hive partition - aggregation on data column
# Original case: test/cpp/integration/test_gpu_execution_multi_format.cpp:2117
# sirius: id = "hive_disabled/aggregate"
# sirius: tags = ["integration", "parquet", "hive_partition", "previously_disabled"]
query ? rowsort
SELECT
  SUM(amount) AS total
FROM
  read_parquet('__FIXTURE_ROOT__/hive-original/h/year=*/month=*/*.parquet', hive_partitioning = true);
----
