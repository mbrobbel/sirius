statement ok
SET disabled_optimizers = 'deliminator,compressed_materialization';

statement ok
CREATE TABLE dim (
  id INTEGER PRIMARY KEY,
  name VARCHAR
);

statement ok
INSERT INTO dim VALUES
  (1, 'a'),
  (2, 'b'),
  (3, 'c'),
  (4, 'd');

statement ok
CREATE TABLE fact (
  k INTEGER,
  grp INTEGER,
  v INTEGER
);

statement ok
INSERT INTO fact VALUES
  (1, 1, 10),
  (1, 1, 30),
  (2, 1, 20),
  (2, 2, 5),
  (3, 2, 50),
  (NULL, 2, 7),
  (5, 3, 100),
  (1, 3, 1),
  (2, 3, 2),
  (3, 3, 3),
  (NULL, 3, 4),
  (4, 1, 8),
  (4, 2, 9),
  (1, 2, 11),
  (2, 1, 12),
  (3, 1, 13),
  (5, NULL, 60),
  (NULL, NULL, 70);

statement ok
CREATE TABLE big (
  okey INTEGER,
  k INTEGER
);

statement ok
INSERT INTO big SELECT
  i,
  i % 4
FROM
  range(3000) t (i);

statement ok
CREATE TABLE dup_build (
  k INTEGER,
  tag VARCHAR
);

statement ok
INSERT INTO dup_build VALUES
  (1, 'x'),
  (1, 'y'),
  (2, 'z'),
  (NULL, 'n');

statement ok
CREATE TABLE empty_fact (
  k INTEGER,
  grp INTEGER,
  v INTEGER
);

statement ok
CHECKPOINT;
